language: php

php:
  - 5.3
  - 5.4

branches:
  only:
    travis
before_script:
  - php vendor/vendors.php
  - sh -c "cd libs/php-sundown && rake submodule compile && sudo rake install"
  - echo "extension=sundown.so" >> `php --ini | grep "Loaded Configuration" | sed -e "s|.*:\s*||"`
